"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateId = validateId;
exports.validateAmount = validateAmount;
const types_1 = require("./types");
function validateId(value, name) {
    if (!value || value.trim() === '') {
        throw new types_1.BayseApiError(0, 'invalid_argument', `${name} cannot be empty`);
    }
}
function validateAmount(amount, currency) {
    const minimum = currency === 'NGN' ? 100 : 1;
    if (amount < minimum) {
        throw new types_1.BayseApiError(0, 'invalid_argument', `Minimum amount is ${currency === 'NGN' ? '₦100' : '$1 USD'}`);
    }
}
//# sourceMappingURL=utils.js.map