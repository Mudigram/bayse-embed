"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BayseClient = void 0;
const client_1 = require("./client");
const events_1 = require("./modules/events");
const markets_1 = require("./modules/markets");
const orders_1 = require("./modules/orders");
const portfolio_1 = require("./modules/portfolio");
const wallet_1 = require("./modules/wallet");
const system_1 = require("./modules/system");
const markets_2 = require("./websocket/markets");
const realtime_1 = require("./websocket/realtime");
// ============================================================
// Bayse SDK — Main Entry Point
//
// Usage:
//   import BayseClient from 'bayse-markets-sdk'
//
//   const bayse = new BayseClient({
//     publicKey: process.env.BAYSE_PUBLIC_KEY!,
//     secretKey: process.env.BAYSE_SECRET_KEY!,
//   })
//
//   const { events } = await bayse.events.getAll({ status: 'open' })
// ============================================================
class BayseClient {
    // More modules get added here in Week 2:
    // public markets: MarketsModule
    // public orders: OrdersModule
    // public portfolio: PortfolioModule
    // public wallet: WalletModule
    // public stream: { markets(): ..., realtime(): ... }
    constructor(config) {
        this.stream = {
            markets: () => new markets_2.MarketsStream(),
            realtime: () => new realtime_1.RealtimeStream(),
        };
        if (!config.publicKey)
            throw new Error('[bayse-markets-sdk] publicKey is required');
        if (!config.secretKey)
            throw new Error('[bayse-markets-sdk] secretKey is required');
        const http = new client_1.BayseHttpClient(config);
        this.events = new events_1.EventsModule(http);
        this.markets = new markets_1.MarketsModule(http);
        this.orders = new orders_1.OrdersModule(http);
        this.portfolio = new portfolio_1.PortfolioModule(http);
        this.wallet = new wallet_1.WalletsModule(http);
        this.system = new system_1.SystemModule(http);
    }
}
exports.BayseClient = BayseClient;
// Named exports for types — consumers can import these too
__exportStar(require("./types"), exports);
// Default export
exports.default = BayseClient;
//# sourceMappingURL=index.js.map