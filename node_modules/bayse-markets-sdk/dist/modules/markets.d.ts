import { BayseHttpClient } from '../client';
import { Ticker, OrderBook, Trade, QuoteBody, QuoteResponse, ListTradesParams, Currency } from '../types';
export declare class MarketsModule {
    private client;
    constructor(client: BayseHttpClient);
    /**
     * Get ticker for a market
     *
     * @example
     * const ticker = await bayse.markets.getTicker('a1b2c3d4-...')
     */
    getTicker(marketId: string): Promise<Ticker>;
    /**
     * Get order book for a market
     *
     * @example
     * const orderBook = await bayse.markets.getOrderBook(['outcome-id-1', 'outcome-id-2'], 'USD');
     */
    getOrderBook(outcomeIds: string[], currency?: Currency): Promise<OrderBook[]>;
    /**
     * Get trades for a market
     *
     * @example
     * const trades = await bayse.markets.getTrades({ marketId: 'a1b2c3d4-...' })
     */
    getTrades(params?: ListTradesParams): Promise<Trade[]>;
    /**
     * Get quote for a market
     *
     * @example
     * const quote = await bayse.markets.getQuote('a1b2c3d4-...', 'b1c2d3e4-...', { outcomeId: 'c1d2e3f4-...', quantity: 10 });
     */
    getQuote(eventId: string, marketId: string, body: QuoteBody): Promise<QuoteResponse>;
}
//# sourceMappingURL=markets.d.ts.map