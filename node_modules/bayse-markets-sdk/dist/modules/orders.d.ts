import { BayseHttpClient } from '../client';
import { PlaceOrderBody, PlaceOrderResponse, PaginatedResponse, ClobOrder, AmmOrder, ListOrdersParams } from '../types';
/**
 * Orders Module
 */
export declare class OrdersModule {
    private client;
    constructor(client: BayseHttpClient);
    /**
     * Get an order by its UUID.
     *
     * @example
     * const order = await bayse.orders.getById('a1b2c3d4-...')
     */
    getById(orderId: string): Promise<ClobOrder>;
    /**
     * List all orders
     *
     * @example
     * const orders = await bayse.orders.getAll({ page: 1, size: 10 });
     */
    getAll(params?: ListOrdersParams): Promise<PaginatedResponse<ClobOrder | AmmOrder>>;
    /**
     * Place an order
     *
     * @example
     * const order = await bayse.orders.place('a1b2c3d4-...', 'b1c2d3e4-...', {
     *   side: 'BUY',
     *   outcome: 'YES',
     *   amount: 10,
     *   currency: 'USD',
     *   price: 10,
     * });
     */
    place(eventId: string, marketId: string, body: PlaceOrderBody): Promise<PlaceOrderResponse>;
    /**
     * Cancel an order
     *
     * @example
     * const order = await bayse.orders.cancel('a1b2c3d4-...')
     */
    cancel(orderId: string): Promise<unknown>;
}
//# sourceMappingURL=orders.d.ts.map